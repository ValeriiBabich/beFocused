<!DOCTYPE html>
<html>
  <head>
    <title>BeFocused</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <style>
  @import url('https://fonts.googleapis.com/css2?family=Poppins&display=swap');
    #nav-menu {
      justify-content: space-between;
      margin-left: -10px;
      box-shadow: 0px 12px 0px 1px rgba(0, 0, 255, .2);
      position: fixed;
      background: #ccc;
      top:0;
      width: 100%;
      padding-left: 20px;
      font-size: 23px;
      font-family: 'Poppins', sans-serif;
    }

    .befocused{
    }

    .about{
      text-decoration:none;
      margin-left: 85%;
    }
  </style>
  
  <body>
  
    <div class="menu">
      <nav id="nav-menu">
        <span class="befocused" onclick="window.location.href = '/main';">BeFocused</span>
        <span class="about"><%= link_to "ⓘ About", '/about' %></span>
      </nav>
    </div>
    <%= yield %>
  </body>
</html>
